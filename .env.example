# ==============================================================================
# FGN Savings Bond Application - Environment Configuration
# ==============================================================================
#
# SETUP INSTRUCTIONS:
#   1. Copy this file: cp .env.example .env
#   2. Update values below (especially passwords for production!)
#   3. Run: docker-compose up -d
#
# SECURITY WARNING:
#   - Never commit .env to version control
#   - Change ALL default passwords before production deployment
#   - See docs/SECURITY.md for production checklist
#
# ==============================================================================

# ==============================================================================
# MongoDB Configuration
# ==============================================================================

# MongoDB connection string
# - For Docker Compose: use 'mongodb' as hostname (service name)
# - For local development: use 'localhost'
# - For MongoDB Atlas: use your atlas connection string
MONGO_URI=mongodb://admin:password@mongodb:27017/fgn_bonds?authSource=admin

# Database name for storing bond applications
DB_NAME=fgn_bonds

# Collection name within the database
COLLECTION_NAME=applications

# ==============================================================================
# MongoDB Docker Initialization
# ==============================================================================
# These credentials are used by MongoDB container on first startup
# IMPORTANT: Change these for production!

# MongoDB admin username
MONGO_INITDB_ROOT_USERNAME=admin

# MongoDB admin password
# Generate strong password: openssl rand -base64 32
MONGO_INITDB_ROOT_PASSWORD=password

# ==============================================================================
# Application Settings
# ==============================================================================

# Environment mode: 'development' or 'production'
# - development: verbose logging, debug features enabled
# - production: optimized settings, minimal logging
ENVIRONMENT=development

# Secret key for session management
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here_change_in_production

# ==============================================================================
# Admin Dashboard Authentication
# ==============================================================================

# Admin username for dashboard login
ADMIN_USERNAME=admin

# Admin password hash (SHA-256)
#
# DEFAULT: Hash of 'admin123' - CHANGE THIS FOR PRODUCTION!
#
# Generate new hash:
#   macOS/Linux: echo -n "YourPassword" | shasum -a 256 | cut -d' ' -f1
#   Windows:     python -c "import hashlib; print(hashlib.sha256('YourPassword'.encode()).hexdigest())"
#   Python:      import hashlib; hashlib.sha256("YourPassword".encode()).hexdigest()
#
ADMIN_PASSWORD_HASH=240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9
